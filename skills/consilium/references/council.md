# Консилиум: состав, участники, адвокат

Запрос «консилиум» или перечисление нескольких моделей → один и тот же промпт параллельно каждой модели.

## Выбор типа перед запуском

Единая цепочка определения режима и надстроек — одна и та же в интерактиве и в фоне:

1. Явно названный режим или надстройка в запросе → берём как есть, вопросов нет.
2. Иначе режим — по **глаголу запроса**: «оцени, найди проблемы, проревьюй» → ревью;
   «предложи, выбери подход, как лучше сделать» → дизайн-совет (`references/design.md`).
   Артефакт — только тай-брейк при неоднозначном глаголе: «вот план — предложите лучше»
   это дизайн, несмотря на артефакт.
3. Всё ещё неясно → в интерактиве отдельный вызов AskUserQuestion:
   «Ревью артефакта» / «Дизайн-совет (предложить решения)»; в фоне — ревью по умолчанию
   с пометкой «режим не уточнялся» в вердикте.
4. **Надстройки** (multiSelect, header «Консилиум») — отдельным вызовом (вторым, если режим
   спрашивался) и только для режима ревью: «Раунды верификации», «Совещание до консенсуса»,
   «Адвокат дьявола», «Премортем». Зависимые вопросы не объединяй в один вызов: состав
   второго определяется ответом на первый. Для дизайн-совета надстройки не спрашивай:
   перекрёстное ревью и итерации встроены в протокол, адвокат и премортем — по явному запросу.
   В фоне — без надстроек, пометка «тип не уточнялся».

- Выбрана опция «Раунды верификации» без числа → K=2 (раунд 1 — само ревью, K=1 был бы no-op).
- Ничего не выбрано → обычный консилиум. «Other» пользователь использует для свободного ввода
  (состав, effort, число раундов) — учитывай его текст как прямое указание.
- Упоминание **любой** надстройки, числа раундов или режима подавляет соответствующий вопрос:
  «полный консилиум», «с адвокатом», «пусть обсудят», «в K раундов», «дизайн-консилиум» —
  пользователь уже сделал выбор.
- Уточнение только состава или effort («консилиум с xhigh») тип не задаёт → вопрос показывается.
- Опцию «Премортем» показывай только для ревью документов и архитектуры — в код-ревью её нет.
- Фоновая/неинтерактивная сессия → вопросы не задаются: та же цепочка (глагол → артефакт
  как тай-брейк), дефолты из шагов 3–4, пометка «тип не уточнялся».
- Тип определён — но участники ещё не запускаются: для дизайн-совета и ревью архитектурных
  решений сначала этап 0 «Требования» (`references/chair.md`). Проектирование и оценка без чисел
  заказчика — самый дорогой дефект цикла: он обнаруживается через часы и обесценивает всё,
  что сделано до него.

## Базовый запуск

- Состав не указан → sol + 5.5 (codex) + opus (Claude-сабагент), все high. Указан → список пользователя.
- Preflight codex (симметрично проверкам cursor и клод-агентов, обе команды — доли секунды):
  `codex login status` (не «Logged in» → участник исключается, пользователю сообщается сразу,
  а не после запуска остальных) и наличие слага в `codex debug models`. Истёкшая авторизация
  иначе выясняется после старта всего состава, когда время уже потрачено.
- «Консилиум с <effort>» без имён моделей → дефолтный состав на этом effort:
  «консилиум с xhigh» = sol-xhigh + 5.5-xhigh + opus-xhigh. Effort выше потолка модели →
  ближайший поддерживаемый, с пометкой в вердикте (у 5.5 потолок xhigh).
- Все вызовы запускай параллельно; артефакт, идентификатор и критерий сбоя — по вендору
  (детали в вендорских секциях ниже):

| Вендор | Артефакт итога | Идентификатор для продолжения | Критерий сбоя |
|---|---|---|---|
| codex | `-o`-файл (уникальное имя) | session id (строка `session id:` в stdout) | exit != 0 или пустой `-o` |
| opus (Claude) | финальный отчёт сабагента | agentId из результата запуска | ошибка в notification или пустой отчёт |
| grok (cursor) | редирект stdout в файл | chatId (create-chat до запуска) | exit != 0; при exit 0 и пустом out-файле смотри err-файл и решай сам |

- Идентификатор каждого участника фиксируй сразу при запуске — задним числом искать неудобно,
  а между вызовами Bash tool переменные не живут: сохраняй литералом.
- Различай валидность результата и пригодность к продолжению. Валидность — строго по критерию
  сбоя своего вендора из таблицы (общего правила «пустой = сбой» нет). Результат есть,
  но continuation-id не добыт (session id не напечатан, agentId не вернулся, chatId потерян) →
  участник валиден, но **статичен**. Фолбэк статичного/мёртвого участника — по вендору:
  codex — свежий `exec` с его прошлой позицией текстом (каркас resume — в SKILL.md);
  opus — свежий сабагент (см. секцию клод-участников); grok — свежий `create-chat`-прогон
  с позицией текстом. Везде пометка «память рассуждений потеряна».
- Промпт у всех участников идентичный — иначе расхождения ревью не отличить от разницы моделей.
- Участник упал (критерий сбоя своего вендора) → один повторный запуск;
  упал повторно → синтезируй по оставшимся и явно пометь, кто выбыл.
  Инфраструктурный сбой запуска (хук, отсутствие команды) — не попытка модели: чини и запускай.
- Синтез после завершения **всех** прогонов:
  1. Совпавшие находки (2+ модели) — верхним блоком как приоритет проверки;
     уверенность — только после сверки с кодом: модели могут разделять общую ошибку.
  2. Уникальные находки — со своей оценкой: правдоподобно или похоже на галлюцинацию.
  3. Прямые противоречия — покажи оба мнения, не выбирай молча.
- Не вываливай сырые ревью подряд — пользователь ждёт один сведённый вердикт.

## Протокол цикла (лог председателя)

Для любого цикла с раундами, совещанием или несколькими участниками веди append-only лог
в **постоянном** каталоге (scratchpad привязан к UUID сессии и после перезапуска не адресуем).

Три разных вещи, которые легко перепутать: **входные материалы участникам** (`00-task.md`
и документы — их читают участники), **файл промпта** (его подаёт на stdin председатель)
и **лог председателя** (его не видит никто, кроме пользователя). Место лога зависит от первого:

- входные материалы кладутся внутрь репозитория (частый случай — воркспейс grok, см. секцию
  cursor-участников) → лог веди **вне репозитория**: `~/.claude/projects/<слаг проекта>/consilium/`
  (per-project каталог Claude Code). Участники запускаются с `-C <корень проекта>` и читают файлы
  репо; git-ignore защищает от утечки в diff, но не от чтения агентом, а в логе раскрыт маппинг
  анонимности;
- входные материалы целиком в scratchpad → допустим `<корень проекта>/.claude/consilium/<дата>-<слаг цели>.md`
  при условии, что `.claude` игнорируется git'ом (`git check-ignore .claude/consilium/x`) — иначе
  лог попадёт в скоуп `review --uncommitted` и утечёт участникам.

При создании напечатай пользователю абсолютный путь.
Одиночное ревью без раундов и совещания не логируется.
Когда именно писать, два формата лога, восстановление после перезапуска, тайминги, счётчики
и чекпоинт по объёму — разделы «Лог: когда писать», «Восстановление цикла» и «Тайминги и счётчики»
в `references/chair.md` (они нужны любому логируемому циклу, включая код-ревью; остальные разделы
`chair.md` — только для дизайн-совета и ревью архитектурных документов, см. таблицу режимов
в SKILL.md). Раздел «Подписи и ответственность» — `references/finalization.md`, там же, где кворум:
для код-ревью он не применяется.

События пиши по мере появления данных, одна строка на событие:

- старт: цель, режим, надстройки и K, состав (модели+effort), cwd (`-C`/worktree),
  session-refs и BASE/HEAD при MR;
- запуск участника: участник → id (session id / agentId / chatId) → путь его артефакта;
- синтез-1: маппинг анонимных меток → участники (появляется только здесь), сведённые ID,
  противоречия, очередь вопросов;
- голоса верификации по ID, выбывшие находки;
- совещание: «итерация i из K», кто сменил или снял позицию, консенсусы;
- вопросы пользователя участникам и краткие ответы;
- для дизайн-совета: путь к текущей редакции решения после каждой итерации;
- финальный вердикт: что применено, что отклонено и почему.

Зачем: восстановление цикла после перезапуска сессии (маппинги, id и состояние очереди
больше нигде не живут), аудит вердикта задним числом, сырьё для ADR в дизайн-режиме.
**Участникам лог не показывать и путь к нему не передавать** — в нём раскрыт маппинг анонимности.
Участник получает только явно переданные ему пути; каталог лога в этот список не входит никогда,
поэтому и держим его отдельно от входных материалов.

## Клод-участники (смешанный консилиум)

Модели Claude участвуют наравне с codex — второй вендор, некоррелированные слепые пятна.

| Псевдоним | Запуск | Effort |
|---|---|---|
| opus | Agent tool, subagent_type `claude-reviewer` | high |
| opus-xhigh | Agent tool, subagent_type `claude-reviewer-xhigh` | xhigh |
| sonnet / fable | Agent tool, general-purpose + `model: sonnet/fable` | сессионный |

- Промпт раунда 1 тот же, что у codex-участников (роль зашита в агент-определении);
  запуск в фоне, параллельно с codex-вызовами.
- Агент-определение `claude-reviewer` требует «финальный отчёт — только нумерованный список
  находок». Для ревью это то, что нужно, но для **неревьюерских ролей** (автор решения
  в дизайн-совете, бюллетень этапа 2 с `RANKING:`, премортем, бюллетень кворума) формат
  конфликтует с требуемым, а системная инструкция агента сильнее указаний в промпте.
  Поэтому для таких ролей запускай general-purpose с `model: opus` — не пытайся переспорить
  системный промпт строкой «формат ниже важнее». Цена: у general-purpose effort сессионный,
  то есть opus-xhigh для неревьюерской роли так не получить — нужен свой agent-файл
  (по образцу `claude-reviewer-xhigh.md`, но без требования «только список находок»).
- Итог — финальный отчёт сабагента (аналог `-o`-файла); аналог session id — agentId из ответа.
  Отчёт возвращается **только в контекст**, поэтому сохраняй его в файл scratchpad в том же ходе,
  в котором получил (правило и путь восстановления — «Обработка результата», пункт 2 в SKILL.md).
  Иначе компактификация длинного цикла стирает голос участника ровно к моменту кворума.
- Совещание: вместо `resume` — SendMessage по agentId из результата запуска. Маппинг
  участник→agentId→effort фиксируй сразу при запуске; agentId не вернулся или SendMessage
  недоступен → участник статичен (фолбэк: свежий сабагент с его находками текстом).
  Ответ придёт task-notification'ом, как у фоновых задач.
  Всё это — в рамках текущей Claude-сессии: codex-сессии живут на диске и переживают
  перезапуск, клод-участник — нет; после перезапуска поднимай свежего агента, передав
  его прошлые находки текстом (память рассуждений при этом теряется — пометь в вердикте).
- Раунды верификации: свежий сабагент (аналог свежей codex-сессии), находки анонимизированы.
- Агент-определения: `~/.claude/agents/claude-reviewer*.md`; новые файлы подхватываются
  на лету, перезапуск сессии не нужен. Диапазон `effort:` — low…max, xhigh валиден.
- Preflight: перед смешанным консилиумом проверь, что агенты `claude-reviewer*` есть в списке
  доступных типов; нет → исключи клод-участника с пометкой «неполный состав».
- Read-only у клод-участников — best-effort: в `tools:` агентов нет Edit/Write, но Bash остаётся;
  жёсткой гарантии уровня codex-санбокса нет.
- Биллинг раздельный: codex — по его способу аутентификации (подписка или API-key),
  клод-участники — Claude-план, grok — Cursor-план (тир `-fast` быстрее и дороже).

## Cursor-участники (grok)

Третий вендор (xAI) — по явному запросу («консилиум с grok», «добавь grok»). Effort зашит в слаг.

| Псевдоним | Слаг `--model` |
|---|---|
| grok | `cursor-grok-4.6-high` |
| grok-xhigh / grok-medium / grok-low | `cursor-grok-4.6-xhigh` / `-medium` / `-low` |
| grok-4.5 | `cursor-grok-4.5-high` (потолок 4.5 — high, xhigh нет) |
| grok-4.5-medium / grok-4.5-low | `cursor-grok-4.5-medium` / `-low` |
| grok-…-fast | суффикс `-fast` (быстрый тир, дороже) |

- Preflight: `cursor-agent status` (авторизация) и наличие слага в `cursor-agent --list-models`;
  не залогинен или слага нет → исключи участника с пометкой «неполный состав».
  Суффиксы слагов догадкой не конструируй.
- До запуска создай chatId отдельным вызовом (`cursor-agent create-chat`, лимит — параметр
  timeout Bash tool) и зафиксируй значение литералом в маппинге участник→chatId:
  переменная `$CHAT_ID` следующий вызов Bash tool не переживёт. Сам запуск оформляй
  скриптом в scratchpad (`zsh <файл>`): инлайновый `"$(cat prompt.txt)"` режется хуком lean-ctx.
  Интерактивный `cursor-agent ls` в Bash неисполним
  (Ink падает без TTY: «Raw mode is not supported»); незалогиненный или недоверенный запуск
  виснет так же — поэтому `--trust` в команде обязателен.
- Запуск — только скриптом, готовая обёртка в самом скилле:
  `zsh ~/.claude/skills/consilium/scripts/cursor-run.sh <слаг> <воркспейс> <prompt.md> <итог.md> <chatId>`
  (chatId — **литералом** с предыдущего шага, вызов — в run_in_background). `$(cat …)` внутри
  скрипта легален: хук инспектирует только инлайновую команду Bash tool. Промпт кладём в файл —
  кавычки пользователя в аргументе означают инъекцию; длинные списки находок тоже файлом.
  Что делает обёртка, если понадобится своя версия:

```bash
#!/bin/zsh
export LEAN_CTX_DISABLED=1
cursor-agent -p --output-format=text --mode ask --sandbox enabled --trust \
  --model cursor-grok-4.6-high --workspace <корень репо> --resume <chatId-литералом> \
  -- "$(cat <scratchpad>/prompt.txt)" > <scratchpad>/grok-out.md 2> <scratchpad>/grok-err.log
```

- `--resume` с пустым значением опасен: параметр опционален, и cursor-agent уйдёт
  в интерактивный выбор сессии — зависание без TTY.
- Входные материалы для grok клади **внутрь воркспейса** — в git-ignored каталог репозитория,
  например `<репо>/.claude/<цель>/`: постановка и требования этапа 0 — файлом `00-task.md`,
  документы на ревью и длинные списки находок — отдельными файлами рядом,
  а не в scratchpad: доступ `--sandbox enabled --workspace <репо>` к `/private/tmp/...`
  не гарантирован. Codex читает и вне `-C`, но путь всем участникам давай **один и тот же** —
  иначе промпты перестают быть симметричными. Побочное следствие: вход лежит в репо, значит лог
  председателя уходит за его пределы (см. «Протокол цикла»).

- stderr — отдельным файлом, иначе текст ошибки уедет в синтез как «находки».
  Падение grok-участника определяй по exit code, не по пустоте файла.
- Read-only у grok — best-effort, как у клод-участников: `--mode ask` документирован как
  read-only + `--sandbox enabled`, но гарантии уровня codex `-s read-only` нет.
  `--force`/`--yolo` запрещены.
- Совещание: новый скрипт на каждый вопрос, внутри `--resume <его chatId литералом>` —
  сессия продолжается (переменные между вызовами не живут — только литерал).
  ChatId потерян → участник статичен: свежий `create-chat`-прогон с его позицией текстом.
  Раунды верификации — свежий `create-chat`, не resume.
- `LEAN_CTX_DISABLED=1` обязателен и здесь: у cursor свой lean-ctx-хук в `~/.cursor/hooks.json`.
- Кросс-вендорный адвокат дьявола: grok — хороший кандидат для атаки на GPT+Claude консенсус.

## Адвокат дьявола

По запросу («с адвокатом дьявола», «пусть кто-то будет против») — +1 вызов с особой ролью.
Исключение из правила «промпт у всех идентичный» (как и адресные вопросы совещания —
симметрия обязательна только в раунде 1). Работает и после одиночного ревью.

- Запускается **после** синтеза: получает артефакт и сведённый вердикт (находки анонимны).
  В комбинации с раундами — после пересинтеза; полный порядок режимов — в `references/deliberation.md`.
- Промпт: «Консенсус ревьюеров — ниже. Атакуй его: докажи, что подтверждённые находки ложные
  или неважные, а главная проблема пропущена. Ищи общее слепое пятно ревьюеров. Сверяйся с кодом.
  Атака по пункту не удалась → так и скажи: выдержал».
- Модель — сильнейшая из доступных (дефолт sol-high): атаковать труднее, чем находить.
  Чисто-GPT состав консилиума → предпочти opus (и наоборот): кросс-вендорная атака
  бьёт по общим слепым пятнам семьи моделей.
- **Число адвокатов — параметр.** Дефолт 1 (чтобы не удорожать типовые прогоны); пользователь
  задаёт больше словами («два адвоката дьявола», «атака ×3»), в AskUserQuestion надстроек это
  указывается в свободном вводе. При N ≥ 2 адвокаты берутся из **разных вендоров** — в этом
  и смысл увеличения, иначе растёт цена без роста разнообразия. Автор решения не может быть
  своим адвокатом. Результаты сводятся как в консилиуме: совпавшие атаки — верхним блоком,
  уникальные — с пометкой автора и оценкой правдоподобия председателем.
- **В дизайн-режиме атаковать нечего** — находок нет, есть выбранное решение. Перед запуском
  председатель выписывает отдельным блоком тезисы, на которых независимо сошлись **все**
  участники (обычно 3–5), и передаёт их как основную мишень: общее согласие — самое вероятное
  слепое пятно совета. Плюс список того, что автор отклонил и почему: согласие чаще всего
  достигается умолчанием именно там.
- В вердикт: что выдержало атаку (это крепче), что не выдержало (пометить), что найдено нового.
- Зачем: симметричные раунды не ловят общее слепое пятно — если ошибаются все, большинство его «подтвердит».
