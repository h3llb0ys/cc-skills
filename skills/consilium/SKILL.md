---
name: consilium
description: "Use when the user asks for an external review, second opinion, or solution proposals from CLI agents (codex/GPT, Claude subagents, cursor-agent/grok) — on code (diff, branch, commit, MR), an architecture decision, plan, or spec, or an open design question. Triggers — «codex», «консилиум», «ревью через codex/grok/opus», «второе мнение», «пусть обсудят / совещание до консенсуса», «спроси у sol/opus/grok», «дизайн-консилиум / пусть предложат решения», «премортем плана/решения», «с адвокатом дьявола», «упростите решение / что тут можно выкинуть», «A или B — что выбрать», «ревьюй до устранения замечаний», «я поправлю — проверишь снова»."
---

# Consilium

## Overview

Совет консольных агентов для внешнего ревью: codex CLI (GPT), Claude-сабагенты (opus), cursor-agent (grok).
Два верхнеуровневых режима: **ревью артефакта** (код-ревью git-изменений; документы —
архитектурные решения, планы, спеки) и **выработка решения** (дизайн-совет, `references/design.md`).
Отдельно стоит **вопрос по развилке** — оба варианта уже сформулированы, нужен выбор, а не ревью
и не проработка: один заход без этапов, строка в «Пакетах чтения».
Мультимодельные надстройки — по запросу, см. «Мультимодельные режимы» и «Пакеты чтения».
Codex запускается только неинтерактивно (`codex exec`) и только на чтение — он рецензент, а не исполнитель.
Это общее правило для всех вендоров: интерактивные формы и TUI (`codex` без `exec`, `cursor-agent ls`)
в Bash не запускай — Ink падает без TTY и вешает вызов. Пробы CLI (свои и в промптах
участникам) ограничивай таймаутом: в Bash tool — параметром `timeout` самого вызова;
команды `timeout(1)` на macOS нет — внешним timeout только если он установлен.
Codex сам читает `AGENTS.md` репозитория — это осознанно. `--ignore-rules` отключает execpolicy
`.rules` (к `AGENTS.md` отношения не имеет) — тоже не добавляй.
Read-only = санбокс блокирует запись через shell-команды модели; свои state-файлы сессии codex пишет всё равно.

## Выбор модели

Модель и усилие — **раздельные** параметры: `-m <слаг>` и `-c model_reasoning_effort=<effort>`.
Слитный слаг вида `-m sol-high` невалиден: парсер его пропустит, но вызов упадёт на API.
Пользователь называет модель коротко («tera-medium», «luna») → разверни по таблице: слаг + effort из суффикса.

| Псевдоним | Слаг для `-m` | Efforts | Что за модель |
|---|---|---|---|
| sol | `gpt-5.6-sol` | low/medium/high/xhigh/max/ultra | флагман для агентного кодинга |
| tera, terra | `gpt-5.6-terra` | low/medium/high/xhigh/max/ultra | сбалансированная повседневная |
| luna | `gpt-5.6-luna` | low/medium/high/xhigh/max | быстрая и дешёвая |
| 5.5 | `gpt-5.5` | low/medium/high/xhigh | frontier: код + ресёрч |
| 5.4 | `gpt-5.4` | low/medium/high/xhigh | крепкая повседневная |
| mini | `gpt-5.4-mini` | low/medium/high/xhigh | маленькая, для простых задач |

- Модель не указана → `-m gpt-5.6-sol -c model_reasoning_effort=high`.
- Effort не указан → high. Это политика скилла (осознанный компромисс цена/качество).
  Неявный дефолт берётся из `~/.codex/config.toml` пользователя и может быть любым —
  поэтому `-m` и `-c model_reasoning_effort` задавай всегда явно.
- Незнакомое имя или слаг вне таблицы → сверься с каталогом; сырой вывод ~300 КБ, фильтруй:
  `codex debug models | python3 -c "import json,sys; [print(m['slug'], [e['effort'] for e in m['supported_reasoning_levels']]) for m in json.load(sys.stdin)['models'] if m['visibility']=='list']"`

Ниже `$MODEL` = `-m gpt-5.6-sol -c model_reasoning_effort=high` (или выбор пользователя).

## Промпт для участников

Собирай из шести частей — механика одна для любого вендора (codex, opus, grok):

1. Стандартная структура из рецептов ниже: роль, фокус, формат ответа. Плюс язык: «отвечай
   на языке этого промпта» — в смешанном составе модели иначе отвечают вразнобой, и сведённый
   вердикт получается двуязычным.
   Запрос широкий («найди проблемы», «где узкие места», «что сломается под нагрузкой») → до запуска
   выясни **ось ранжирования**: что именно оптимизируется и по чему сортируется серьёзность
   (латентность горячего пути, throughput записи, стоимость владения, безопасность). Вопрос
   блокирующий по признаку из `references/chair.md`, «Вопросы заказчику» — промпт без оси
   не собрать; в фоне выведи ось из цели запроса и пометь предположение в вердикте. Ось идёт
   в промпт дословно, и «блокер» считается по ней. Без оси тема читается буквально — «любой
   отказ», — и совет смешает горячий путь с операционкой и security, а заказчик забракует
   половину находок как шелуху.
2. **Все** дополнительные пожелания пользователя из запроса — дословно
   («дополнительно ищи кривые буквы», «смотри только на конкурентность»).
   Пожелания пользователя приоритетнее стандартного фокуса и могут его сужать или заменять.
   Дословно идёт **содержательная** часть. Состав совета, модели, effort, число раундов
   и условия согласования — это инструкции председателю: их из цитаты убирают или заменяют
   нейтральным описанием цикла («цикл итеративный, автор правит документ между раундами»).
   Участник, узнавший из цитаты о коллегах, тратит ход на состав совета и отказывается
   расписываться за отсутствующих; имена моделей знает только пользователь (`references/deliberation.md`).
3. Указание проверять локально: «сверяйся с локальным CLI/кодом; веб-поиск — только если локально
   проверить нельзя» — иначе модель уходит гуглить документацию и прогон растягивается в разы.
   Третий уровень — **поведение внешней системы** (СУБД, брокер, библиотека): его решает прогон,
   а не документация. Проси у участника точную одноразовую команду («что запустить, чтобы это
   проверить»); прогоняет её председатель — условия и границы в `references/chair.md`,
   «Проверка фактов по коду». В ролях со строгим форматом этот уровень не запрашивается.
4. Проверка фактов: «утверждения о коде, схеме или конфигурации — из предмета **и из этого
   промпта** — проверь и отметь неверные», плюс блок `ПРОВЕРКА ФАКТОВ` в формате ответа: документ
   и пересказ легко воспроизводят то, чего в системе нет. Шире «автора» намеренно: на код-ревью
   автор ничего не пишет, и проверяемые факты приносит блок контекста — то есть сам председатель.
5. Право спросить заказчика: «блок `ВОПРОСЫ ЗАКАЗЧИКУ` — 0–3 пункта, не обязателен».
   Участник изолирован от пользователя и недостающее достраивает догадкой; фильтр — `references/chair.md`.
6. Давление на объём — **отдельным блоком** `УПРОЩЕНИЯ`, не строкой в списке фокусов:
   «0–3 требования предмета, отказ от которых убрал бы больше всего кода. По каждому: что исчезнет
   из реализации и что потеряет пользователь. Это не находки — дефектом их обосновывать не нужно».
   Почему именно отдельная рамка и почему `0–3` — `references/deliberation.md`, «Упроститель».

**Части 1–3 — у любого участника. Части 4–6 — только у ревью артефакта в раунде 1.** Роли
со своим строгим форматом (раунды верификации, адвокат дьявола, премортем, бюллетени кворума
и дизайн-совета) берут 1–3, вопрос по развилке — 1–3 плюс часть 5 (`ВОПРОСЫ ЗАКАЗЧИКУ`
входит в его собственный формат): формат задан ролью, а лишние блоки ломают
подсчёт голосов. Клод-участник для частей 4–6 — `claude-council`/`claude-council-xhigh`;
`claude-reviewer*` отдаёт
голый список находок и всё сверх него срежет — включая строку про пустой результат (`references/cycle-state.md`).

Промпт от ~40 строк пиши файлом и подавай перенаправлением ввода — иначе при внешнем сбое
(обновление CLI, таймаут) heredoc придётся набирать заново, а слегка другой текст делает
результаты несравнимыми:

- codex: `zsh ~/.claude/skills/consilium/scripts/codex-run.sh <слаг> <effort> <репо> <prompt.md> <итог.md> [session-id]`
- cursor: `zsh ~/.claude/skills/consilium/scripts/cursor-run.sh <слаг> <воркспейс> <prompt.md> <итог.md> <chatId>`

Обе обёртки отказываются работать, если out-файл уже существует, — ретрай не затирает артефакт
прошлого прогона. Дальше вендорское: `codex-run.sh` ставит `-s read-only`, кладёт stdout целиком
в `<итог>.stdout` и печатает `session id:`; `cursor-run.sh` пишет ответ прямо в `<итог>`,
stderr — в `<итог>.err`, а continuation-id у него свой (chatId, добывается до запуска)
и в выводе не печатается. Собственный пайп к codex не приделывай:
`| tail`/`| grep` обрезают баннер, и id участника теряется вместе с ним.
Запуск в фоне (`run_in_background`) — конца прогона не жди: codex печатает баннер в первые
секунды, и `grep 'session id:' <итог>.stdout` отдаёт id, пока участник ещё работает. Строки
там пока нет → повтори чтение через несколько секунд, а не откладывай запись лога до финала.
Ручная сборка команды нужна только там, где скрипт не подходит: `review` со скоуп-флагами
и разовые пробы CLI.

## Каркас вызова

Типовой запуск участника уже собран: `scripts/codex-run.sh` (и `scripts/cursor-run.sh` для grok) —
см. «Промпт для участников». Ниже — из чего он состоит и что менять в нестандартных случаях.

Канонический каркас (строго до сабкоманды обязательны только `-C`/`-s`):

```
codex exec -C <корень репо> -s read-only $MODEL [review] -o <scratchpad>/<уникальный файл>.md …
```

- Строго parent — только `-C`/`-s` (после `review`/`resume` — exit 2). `-m`/`-c`/`-o` есть
  и у сабкоманд; канон: `-m`/`-c` до сабкоманды, `-o` для `review` — после неё
  (parent-`-o` проверен и работает для `exec` и `resume`, для пути `review` — не подтверждён).
- Без явного `-s read-only` санбокс берётся из конфига пользователя — read-only не гарантирован.
- `<scratchpad>` — плейсхолдер: подставь **абсолютный** путь scratchpad-каталога сессии
  (нет такого — создай через `mktemp -d`). Каталог лога сюда не подставляй: путь `-o` виден
  в командной строке; логируемый цикл снимает **копию** артефакта рядом с логом при чтении
  результата (`references/cycle-state.md`, «Протокол цикла»). Имя файла уникально на каждый прогон и ретрай,
  иначе рискуешь прочитать итог прошлого запуска.
- Делимитер heredoc генерируй уникальным на каждый вызов (например `CX_<случайный суффикс>`)
  и убедись, что такой строки нет в промпте: пожелания пользователя вставляются дословно.
- stdin в Bash tool — pipe: промпт аргументом → закрывай stdin через `</dev/null`, иначе codex ждёт EOF.

## Код-ревью

Скоуп-флаги и позиционный промпт у `review` **несовместимы** (`cannot be used with '[PROMPT]'`). Две формы:

- Скоуп-флаг: `review --uncommitted` | `review --base <ветка>` | `review --commit <sha>`. Промпта
  эта форма не несёт **по построению** — значит не несёт и обязательных блоков (`ПРОВЕРКА ФАКТОВ`,
  `УПРОЩЕНИЯ`, строка про пустой результат, язык ответа). Поэтому раунд 1 ревью артефакта идёт
  только через custom-промпт; скоуп-флаг остаётся для прогонов, которым части 4–6 не положены.
- Ревью с фокусом/контекстом → custom-промпт через heredoc (не через кавычки: в пожеланиях
  пользователя могут быть `"` и `$()` — инъекция в shell). Скоуп задавай текстом и точно:
  заранее вычисли merge-base SHA, явно скажи, включать ли незакоммиченное:

```bash
codex exec -C /path/to/repo -s read-only \
  -m gpt-5.6-sol -c model_reasoning_effort=high \
  review -o "<scratchpad>/codex-review-1.md" - <<'CODEX_PROMPT'
Ты — код-ревьюер. Проревьюй изменения текущей ветки относительно merge-base abc1234 (origin/main).
Незакоммиченные изменения не рассматривай.
Контекст: TICKET-1234, рефакторинг ретраев в consumer.
Фокус: корректность, конкурентность, обработка ошибок. Стиль не комментируй.
Формат каждой находки: серьёзность (блокер/риск/замечание), file:line, суть в 1-2 предложениях, предлагаемый фикс.
Находок нет — перечисли, какие места проверил и по какому критерию.
Отвечай на языке этого промпта.
ПРОВЕРКА ФАКТОВ: утверждения о коде, схеме, конфигурации проверь и отметь неверные с file:line.
УПРОЩЕНИЯ: 0-3 куска этого диффа, которые можно было не писать. По каждому: что исчезнет и что потеряет пользователь.
ВОПРОСЫ ЗАКАЗЧИКУ: 0-3 пункта, не обязателен.
CODEX_PROMPT
```

- Скоуп не назван → при незакоммиченных изменениях ревьюй рабочее дерево, при чистом — против
  дефолтной ветки (проверь, какая существует; несколько remote или нет main/master → спроси пользователя).
### Ревью чужого MR

Требует материализовать его состояние (codex видит только локальный репозиторий).
Способ выбирает пользователь — спроси, молча не создавай worktree и не двигай ветки. Исключение
одно: **состав с grok и чистое дерево** — там выбор фиктивный (fetch-only ему недоступен,
inline двигает дерево пользователя), worktree создаётся дефолтом и сообщается постфактум
(`references/cycle-state.md`, «Базовый запуск»). Способы:
1. **fetch-only** (дефолт, когда спросить нельзя): рабочее дерево не трогается. Fetch — один
   раз до старта участников и не повторять до конца совещания, в уникальные session-refs
   (не в `origin/*`: параллельный fetch соседей не сдвинет вход):
   `S=$(uuidgen); echo "SESSION_REF=$S"; git fetch --no-tags origin "+refs/heads/<дефолтная ветка>:refs/codex-review/$S/base" "+refs/merge-requests/<iid>/head:refs/codex-review/$S/head"`
   — `echo` обязателен: дальше значение используется только литералом, между вызовами
   Bash tool переменные не сохраняются.
   (ветка из fork → второй fetch с URL fork'а). Один раз вычисли BASE/HEAD/merge-base
   (`git rev-parse`, `git merge-base`) и передай в промпт: «рассматривай только diff
   `<MB>..<HEAD>`, контекст файлов читай из `<HEAD>` через git, рабочее дерево игнорируй».
   Файлы на диске — не MR, пометь ограничение в вердикте.
   После всех resume/совещания: `git update-ref -d` обоих session-refs.
2. **inline checkout**: точный контекст, но двигает рабочее дерево — только при чистом
   и с возвратом исходной ветки после ревью.
3. **отдельный worktree**: точный контекст + изоляция. Все вызовы этого ревью — включая
   resume, раунды и совещание — идут с `-C <путь worktree>`: резюмированной сессии нужен
   ревьюируемый код на диске (сам id от cwd не зависит — resume по UUID работает из любого
   `-C`, к cwd привязан только `--last`), поэтому удаляй worktree только после конца
   совещания и всех уточнений:
   `git worktree remove <путь>` (read-only ревью оставляет дерево чистым, remove пройдёт
   без --force; не прошёл → что-то писало в дерево, разберись, не форси).
   Осиротевшие — `git worktree list` + `prune`.
- В промпт: контекст задачи (тикет, что и зачем менялось) плюс пожелания пользователя — codex их сам не знает.

## Ревью документов (архитектура, планы, спеки)

`review` работает только с git-диффами. Для документов используй `codex exec`, промпт — через heredoc (stdin занят промптом, `</dev/null` не нужен):

```bash
codex exec -m gpt-5.6-sol -c model_reasoning_effort=high -s read-only \
  -C /path/to/repo \
  -o "<scratchpad>/codex-doc-review-1.md" - <<'EOF'
Ты — архитектурный ревьюер. Прочитай docs/plans/ticket-1234-plan.md.
Отвечай на языке этого промпта.
Каждая находка несёт серьёзность: блокер | риск | замечание — и file:line или раздел документа.
Формат находки: серьёзность, место, суть в 1-2 предложениях, предлагаемый фикс.
Блокеры: противоречия, дыры в логике, невыполнимые шаги. Риски: отказы, миграции, нагрузка,
обратная совместимость. Упущенное (альтернативы, edge cases, наблюдаемость) — тоже находка
со своей серьёзностью, а не отдельный уровень.
Находок нет — перечисли, какие места проверил и по какому критерию.
ПРОВЕРКА ФАКТОВ: утверждения о коде, схеме, конфигурации — из документа и из этого промпта —
проверь каждое и отметь неверные с file:line.
СОГЛАСОВАННОСТЬ (для многораздельного документа): сверь документ с самим собой — пути и имена
параметров между разделами; имена колонок, типов и значений enum между схемой, миграцией
и задачами; обещание, данное в одном разделе и не выполненное в другом; одно и то же имя символа
в разных разделах; термины, значащие в разных местах разное. Существуют ли символы в коде —
это `ПРОВЕРКА ФАКТОВ`, здесь только согласие документа с самим собой.
УПРОЩЕНИЯ: 0-3 требования плана, отказ от которых убрал бы больше всего кода.
По каждому: что исчезнет из реализации и что потеряет пользователь.
Это не находки — дефектом их обосновывать не нужно.
ВОПРОСЫ ЗАКАЗЧИКУ: 0-3 пункта, не обязателен.
Сверяйся с кодом по ref abc1234 (SHA из preflight, не имя ветки; рабочее дерево игнорируй).
Веб-поиск — только если локально проверить нельзя.
EOF
```

- Cwd (`-C`) вне git-репозитория → добавь `--skip-git-repo-check` (важно расположение cwd, не документа).
- В промпте всегда: путь к документу, роль ревьюера, структура ответа, указание сверяться с кодом,
  контекст задачи (тикет, что и зачем решается) и пожелания пользователя — участник их сам не знает.
- **Назови ревизию, с которой сверяться.** «Сверяйся с кодом» участник исполнит по тому, что нашёл
  на диске, а под `-s read-only` он не может даже `git fetch` — отставший `HEAD` для него неотличим
  от «в коде такого нет», и ложное «неверно» приходит с убедительным `file:line`. Отставание
  меряется в preflight состояния предмета — команда и обработка сбоя там же
  (`references/cycle-state.md`, «Базовый запуск»); здесь только следствие: ненулевое отставание
  идёт в промпт как «сверяйся по ref `<SHA>`, рабочее дерево игнорируй, расхождение с диском
  находкой не считается».
  Ref давай **SHA**, а не имя ветки: `origin/*` двигается под прогонами.
  Подгонять дерево молча нельзя: worktree и checkout — только способом из «Ревью чужого MR»,
  его выбирает пользователь.

**Рецепты задают только фокус и структуру находок — шесть частей конструктора добавляются
к любому из них**, включая язык ответа и все три блока формата (`ПРОВЕРКА ФАКТОВ`, `УПРОЩЕНИЯ`,
`ВОПРОСЫ ЗАКАЗЧИКУ`). Пример копируется дословно чаще,
чем читается конструктор: расхождение примера с конструктором и есть способ потерять проверку фактов.
`СОГЛАСОВАННОСТЬ` — четвёртый блок и только у **многораздельного** документа: он проверяет
соответствие документа самому себе, а `ПРОВЕРКА ФАКТОВ` — соответствие внешнему миру. Одно другое
не покрывает: в одном цикле шесть итераций ревью и апрув пропустили разъехавшиеся пути ручек
и коллизию маршрута, потому что все смотрели логику. На коротком документе блок не требуй —
лишний блок размывает внимание.
Строка про пустой результат идёт с ними в одной группе — у ревью артефакта в **любой** итерации,
кроме бюллетеней и голосований: непустой список находок самоверифицируем по `file:line`,
пустой — нет, а в итеративном режиме именно пустой прогон закрывает цикл.
Шкала серьёзности **находок ревью** одна на весь скилл — `блокер | риск | замечание`; своей
для конкретного режима не заводи. Оговорки кворума (`критичная | некритичная`) и оценки этапа 2
дизайн-совета (`фатальное | слабое`) — про другое, этими словами их не подменяй.

## Мультимодельные режимы

Что стоит за формулировкой пользователя — и во что это обходится. Цена в вызовах: N — число
участников (дефолт 3), время — календарное, при параллельном запуске.

| Просят | Что запускается | Цена |
|---|---|---|
| «проревьюй через codex/opus/grok» | один участник, без раундов | 1 вызов, 5–15 мин |
| «ревьюй до устранения замечаний», «я поправлю — проверишь снова», «до N итераций» | итеративное ревью: один участник через `resume`, ревью каждой новой редакции | 1 вызов на итерацию, дефолт 4; календарно = 5–15 мин на прогон плюс время, которое автор тратит на правки |
| «консилиум», «второе мнение» | N участников параллельно + синтез (дефолт sol + 5.5 + opus, high) | N вызовов, 10–15 мин |
| «консилиум с xhigh», «добавь grok» | тот же цикл, другой состав или effort | N вызовов |
| «полный консилиум», «пусть проверят друг друга», «в K раундов» | + раунды верификации: свежие сессии голосуют по анонимным находкам | ×K вызовов |
| «пусть обсудят», «до консенсуса», «дебаты» | + совещание: живые сессии, адресные вопросы, дефолт 3 итерации | + до 3×N |
| «с адвокатом дьявола», «пусть кто-то будет против» | + атака на консенсус: по возможности кросс-вендорной моделью, иначе по лестнице вендоров (`deliberation.md`); число адвокатов — параметр | +1 и больше |
| «пусть поищут, что выкинуть», «упростите решение», «слишком раздулось» | + упроститель: отдельный прогон против **объёма** решения, после синтеза (`deliberation.md`) | +1 |
| «A или B?», «стоит ли вернуть решение X» — оба варианта уже сформулированы | вопрос по развилке: N участников, один заход, без раундов. Формат: `РЕКОМЕНДАЦИЯ / ПОЧЕМУ / ЧТО УПУСКАЕТ ПРОТИВОПОЛОЖНЫЙ ВАРИАНТ / РИСКИ ВЫБРАННОГО / ВОПРОСЫ ЗАКАЗЧИКУ`. В постановке — история решения честно, включая аргументы прошлого отказа | N вызовов, 10 мин |
| «с премортемом» | + сценарии провала решения; по умолчанию предлагается для документов и архитектуры, по явному запросу запускается и на коде с адаптированной рамкой | +1 и больше |
| «дизайн-консилиум», «пусть предложат решения», «как лучше сделать X» | этапы 0–5: требования → предложения → перекрёстное ревью → выбор → итерации → финал | 2N + итерации, часы |
| «дизайн-консилиум в K раундов» | то же, K итераций доработки с чередованием «доработка → атака» | + K×N |

Комбинируются свободно («консилиум с xhigh, с адвокатом и премортемом»), порядок исполнения
фиксирован — он задан в `deliberation.md`. Что не названо явно, определяется по глаголу запроса
и уточняется вопросом (см. «Выбор типа перед запуском» ниже).

Запрос консилиума, совещания, раундов, итеративного ревью, адвоката дьявола, упростителя,
премортема, вопроса по развилке или вопроса участнику — **сначала прочитай** нужный reference
(в контекст по умолчанию они не загружены):

- `references/cycle-state.md` — состояние цикла: состав и дефолты (sol + 5.5 + opus), preflight,
  таблица вендоров, реестр прогонов, синтез, протокол журнала, восстановление, тайминги.
- `references/deliberation.md` — раунды верификации, совещание до консенсуса, итеративное ревью
  правок автора, вопросы пользователя участникам, премортем и эскалация.
- `references/design.md` — дизайн-совет: участники предлагают решения задачи, перекрёстно
  ревьюят, выбирают лучшее и итеративно улучшают («пусть предложат решения», «как лучше сделать X»).
- `references/chair.md` — роль председателя **до и между запусками**: этап 0 «Требования»,
  проверка фактов по коду и реестр `facts.md`, вопросы заказчику, сверка с исходной задачей.
  Журнал, восстановление цикла и тайминги — не здесь, а в `cycle-state.md`.
- `references/finalization.md` — закрытие цикла: кворум, ревью итогового документа, пакет передачи.

## Выбор типа перед запуском

Единая цепочка определения режима и надстроек — одна и та же в интерактиве и в фоне:

1. Явно названный режим или надстройка в запросе → берём как есть, вопросов нет.
2. Иначе режим — по **глаголу запроса**: «оцени, найди проблемы, проревьюй» → ревью;
   «предложи, выбери подход, как лучше сделать» → дизайн-совет (`references/design.md`).
   Артефакт — только тай-брейк при неоднозначном глаголе: «вот план — предложите лучше»
   это дизайн, несмотря на артефакт. Развилка из **двух уже сформулированных** вариантов
   («A или B», «стоит ли вернуть решение X») — не дизайн-совет: это строка «вопрос по развилке»
   в пакетах чтения ниже, один заход без этапов. Дизайн-совет нужен там, где варианты ещё предстоит
   придумать; на готовой развилке его протокол на 2N вызовов настолько избыточен, что его не запустят.
3. Всё ещё неясно → в интерактиве отдельный вызов AskUserQuestion:
   «Ревью артефакта» / «Дизайн-совет (предложить решения)»; в фоне — ревью по умолчанию
   с пометкой «режим не уточнялся» в вердикте.
4. **Надстройки** (multiSelect, header «Консилиум») — отдельным вызовом (вторым, если режим
   спрашивался) и только для режима ревью: «Раунды верификации», «Совещание до консенсуса»,
   «Адвокат дьявола», «Премортем». Больше четырёх опций в одном вопросе тул не примет, поэтому
   упроститель идёт **вторым вопросом того же вызова** («Запускать упроститель?») — там же, где
   премортем, то есть для документов и архитектуры: у чистого код-ревью нет владельца требований
   (`references/deliberation.md`, «Упроститель»). Правило «не запускать без запроса» этим
   не отменяется — заказчик по-прежнему выбирает сам, но теперь видит, из чего.
   Зависимые вопросы не объединяй в один вызов: состав
   второго определяется ответом на первый. Для дизайн-совета надстройки не спрашивай:
   перекрёстное ревью и итерации встроены в протокол, адвокат и премортем — по явному запросу.
   В фоне — без надстроек, пометка «тип не уточнялся».

- Выбрана опция «Раунды верификации» без числа → K=2 (раунд 1 — само ревью, K=1 был бы no-op).
- Ничего не выбрано → обычный консилиум. «Other» пользователь использует для свободного ввода
  (состав, effort, число раундов) — учитывай его текст как прямое указание.
- Упоминание **любой** надстройки, числа раундов или режима подавляет соответствующий вопрос:
  «полный консилиум», «с адвокатом», «пусть обсудят», «в K раундов», «дизайн-консилиум» —
  пользователь уже сделал выбор.
- Уточнение только состава или effort («консилиум с xhigh») тип не задаёт → вопрос показывается.
- Опцию «Премортем» **предлагай** только для ревью документов и архитектуры — в меню код-ревью её нет.
- **Область ограничивает то, что предлагаешь сам, а не то, что заказчик назвал явно.** Надстройка,
  названная явно и не попавшая в типовую область, **запускается** — с двумя обязательными
  действиями: одной строкой скажи, что адаптировано в промпте и чего от неё ждать меньше
  (премортем на коде: «изменение уехало в прод, через полгода инцидент» вместо «решение принято
  и внедрено»), и пометь это в вердикте. Отказать можно, только когда исполнение ломает инвариант
  скилла — read-only, неизменность дерева во время прогонов, платный прогон без запроса: тогда
  назови инвариант прямо и предложи ближайшую замену. «Не предусмотрено» причиной отказа не является.
- Фоновая/неинтерактивная сессия → вопросы не задаются: та же цепочка (глагол → артефакт
  как тай-брейк), дефолты из шагов 3–4, пометка «тип не уточнялся».
- Тип определён — но участники ещё не запускаются: для дизайн-совета и ревью архитектурных
  решений сначала этап 0 «Требования» (`references/chair.md`). Проектирование и оценка без чисел
  заказчика — самый дорогой дефект цикла: он обнаруживается через часы и обесценивает всё,
  что сделано до него.

## Пакеты чтения

Строка таблицы — предикат по запросу. Читается **файл плюс момент**: файл, названный без момента,
открывают не тогда, когда он нужен. Пакеты объединяются: запрос попал под две строки — читаются обе,
область считается по предмету каждой части.

| Если в запросе | Что читать и когда | Область |
|---|---|---|
| предмет — код | `SKILL.md`; при раундах или нескольких участниках — `cycle-state.md` **до первого запуска** | Этап 0 и кворум не применяются: нет ни автора решения, ни итогового документа. Логируемый цикл ведёт журнал по `cycle-state.md` |
| документ, план, спека | `SKILL.md`; `chair.md` **до запуска участников**; `cycle-state.md`, «Базовый запуск» — **там, где этап 0 велит писать `00-task.md`**: каталог входа задан там | Результат — вердикт, а не документ. Кворум и `finalization.md` только если по итогам сам пишешь документ заказчику. Блокеры и риски → итеративный режим, вердиктом цикл не закрыт |
| «A или B?», «стоит ли вернуть решение X» | `SKILL.md`; `cycle-state.md` **до старта** | Один заход, без этапов дизайна |
| дизайн-совет, «пусть предложат решения», «как лучше сделать X» | `cycle-state.md` и `chair.md` **до старта, до этапа 0**; `design.md` **с этапа 0 и по этапам 1–5**; `finalization.md` **после этапа 5** | Раунды верификации сами не предлагаются: «в K раундов» = K итераций этапа 4. Заказчик назвал их отдельно от числа итераций — запускаются после этапа 2 по выжившим решениям, с пометкой |
| несколько участников, «консилиум», «второе мнение» | `cycle-state.md` **до первого запуска** | Лог и реестр — по порогу цикла (`cycle-state.md`, «Порог: короткий цикл и длинный»). Два участника в переднем плане, без раундов и фоновых запусков, проходят без них; любой из признаков длинного цикла их возвращает |
| «полный консилиум», «пусть проверят друг друга», «в K раундов» | `cycle-state.md` **до первого запуска** (состав, вендоры, журнал); `deliberation.md` **перед раундом 2** | K=2 по умолчанию. Логируется всегда |
| «пусть обсудят», «дебаты», «до консенсуса» | `deliberation.md` **после синтеза-1** | Дефолт 3 итерации |
| «ревьюй до устранения замечаний», «я поправлю — проверишь снова», «до N итераций» | `deliberation.md`, «Итеративное ревью» **сразу после вердикта итерации 1**, до первой правки автора; `chair.md`, если предмет — план или архитектура | Лимит 4 по умолчанию |
| «с адвокатом», «пусть кто-то будет против», «упростите решение», «пусть поищут, что выкинуть», «слишком раздулось», «с премортемом» | `deliberation.md` **после синтеза**; `cycle-state.md` **до запуска роли** (вендор для роли) | Премортем по умолчанию предлагается для документов и архитектуры; **упроститель сам не запускается**, но для документов и архитектуры показывается отдельным вопросом в меню надстроек. Названные явно — запускаются с адаптацией |
| «спроси у sol / opus / grok», вопрос участнику | `deliberation.md`, «Вопросы пользователя участникам» **в момент вопроса** | Лимит итераций не расходуется |
| правка самого скилла, разбор `consilium-refach.md` | `references/maintenance.md` **до первой правки**; `deliberation.md`, «Итеративное ревью» — оттуда механика снимков | Предмет — сам скилл. Публикация только по явной команде заказчика |
| ревью через opus или grok, одиночное | `cycle-state.md` **до запуска** | Запуск клод- и cursor-участников описан только там |

Ревью плана или архитектурного решения вернуло блокеры или риски — вердиктом цикл не закрыт:
правки по ним никто не проверял, а исправляя по замечанию, легко внести новую ошибку. Скажи это
заказчику и предложи итеративный режим; второй прогон молча не запускай.

Вопросы участникам Claude или Grok и смешанное совещание требуют и `cycle-state.md`,
и `deliberation.md`: механика вендора в первом, протокол вопроса — во втором.

Не запускай мультимодельный режим по памяти: механика вендоров различается в деталях
(session id у codex, agentId у клода, chatId у grok), и все детали — там.

## Обработка результата

Пункты 4 и 6–8 — про любой вендор. Вендорское здесь только у codex (пункты 1 и 5, плюс строка
про санбокс в пункте 3) и у клод-сабагентов (пункт 2); артефакты и критерии сбоя остальных
вендоров — таблица в `references/cycle-state.md`.

1. Сначала проверь exit code и что файл `-o` существует и непустой, потом читай его.
   Stdout не используй как содержательный результат (баннер, хуки, прогресс) — бери из него
   только строку `session id:`; итог всегда из `-o`.
2. **Артефакт участника, существующий только в контексте, не существует.** Результат
   клод-сабагента возвращается лишь в контекст оркестратора — сохраняй его в файл scratchpad
   **в том же ходе**, в котором получил, по той же схеме имён, что и `-o` у codex; логируемый
   цикл тем же ходом снимает копию рядом с логом (`references/cycle-state.md`, «Протокол цикла»). Длинный цикл
   почти гарантированно переживает компактификацию, а голоса и вердикты нужны в самом конце.
   Результат всё же потерян → путь восстановления из `agent-<agentId>.jsonl`
   в `references/cycle-state.md`, «Клод-участники».
3. Ревью идёт минуты: `timeout: 600000` или `run_in_background` (параметры Bash tool, не codex).
   Если Bash tool умеет отключать внешнюю песочницу (`dangerouslyDisableSandbox: true`) —
   отключай: вложенный санбокс ломает tool-host codex («timed out negotiating with the
   code-mode host»); чтение-только гарантирует его собственный `-s read-only`.
   Истёкший timeout — инфраструктурный сбой, не падение модели: перезапусти прогон
   через `run_in_background`, повторный полный раунд не нужен.
   Ориентир: участник на xhigh — 5–15 минут на этап, дизайн-цикл с итерациями — часы; сообщай
   пользователю календарное время, а не число вызовов. Цикл растёт сам: на 15 запусках или двух
   часах — обязательная остановка с отчётом и выбором пользователя (чекпоинт по объёму,
   `references/cycle-state.md`). Прогон со статусом «остановлена» и пустым
   файлом — инфраструктурный сбой: перезапускай, а не пересказывай как позицию участника.
4. Перескажи находки пользователю со своей оценкой: с чем согласен, с чем нет и почему.
   Критичные утверждения проверяй сам (пробы парсера, чтение кода) — не транслируй вслепую.
   **Критичное утверждение — то, чья ложность меняет вывод.** Тест один: подставь противоположное
   значение. Изменился бы вердикт, выбор, границы scope, оценка, вывод о безопасности или звено
   «факт → следствие → выбор» — утверждение критичное; не изменился — фоновое. Числа, ссылки
   `file:line`, поведение кода — иллюстрация теста, а не отдельный признак: число, ни на что
   не влияющее, критичным не становится. Критичные проверяются **все**, поштучно; выборка
   допустима только по фону. Исключение одно и оговорено отдельно: артефакт, легший в основу
   решения (классификация, таблица, расчёт), проверяется выборкой обязательного состава —
   `references/chair.md`, «Проверка фактов по коду».
   Блок `ПРОВЕРКА ФАКТОВ` — не исключение: он выглядит проверенным результатом, но это те же
   утверждения участника. Правки чужих `file:line` сверяй командой (`git show <ref>:<путь>`)
   до внесения в документ; при расхождении между участниками по факту прав тот, чей источник
   подтверждается командой, а не тот, кто выразился увереннее.
   Проверяй абсолютным путём или через `git -C`: на рабочий каталог между вызовами Bash tool
   не полагайся — в одних версиях харнесса `cd` переживает вызов, в других сбрасывается,
   и живой файл покажется несуществующим, а находка участника — ложно опровергнутой.
   Находки, уходящие заказчику в план работ или в итоговый документ, проходят **сплошную**
   сверку `file:line`: путь существует и строка в диапазоне файла. Третий признак —
   отбраковочный, а не подтверждающий: место **явно** про другое (соседний модуль, чужая
   структура) — верни находку автору за ссылкой; во всех прочих случаях сверка пройдена.
   Совпадения подстроки не требуй — находка это вывод из места, а не цитата; иначе гейт
   превращается во второй проход по смыслу. Суть находки разваливается редко, ссылка врёт
   регулярно; проверка механическая и стоит минут. Хочешь чужими
   руками — это раунд верификации (`references/deliberation.md`), но он платный, ×N, и запускается
   только по запросу заказчика.
   **Верное не значит нужное.** Прежде чем находка станет правкой, спроси второе: что сломается,
   если этого не делать. Ответ «ничего» — не вносится, даже когда замечание верно; вес ставит
   председатель, градация участника ему сырьё, а не приговор. Собственный документ внешним
   основанием не является: требование, попавшее в него тем же путём, проверяется заново.
   Фундаментальные блокеры в основе решения → вердикт «не согласовано — вернуться после
   правок» (правила эскалации — references/deliberation.md).
5. Уточняющий вопрос → resume строго по session id (строка `session id:` в шапке вывода —
   единственное, что бери из stdout). Каркас тот же, что у обычного вызова, — санбокс и `-o`
   не наследуются молча, задавай явно; вопрос только через heredoc (аргументная форма — инъекция):
   `codex exec -C <репо> -s read-only -m <модель прогона> -c model_reasoning_effort=<его effort> -o <уникальный файл> resume <id> - <<'EOF' … EOF`.
   `--last` не используй: фильтруется по cwd и гонится с параллельными сессиями.
6. Ничего не исправляй по находкам без явной просьбы.
7. Пользователь отменил или сменил запрос → останови фоновые прогоны (TaskStop по их task id)
   или дождись завершения; затем убери worktree/session-refs и пометь результат неполным.
8. В конце цикла проверь осиротевшие процессы и останови **только свои** — механика опознания
   по аргументам своего запуска в `references/cycle-state.md`, «Уборка после цикла».
   Многошаговые запуски оформляй скриптом в scratchpad и вызывай `zsh <файл>`: PreToolUse-хуки среды (свои у Claude Code, codex
   и cursor) инспектируют инлайновую команду и могут её отклонить — содержимое скрипта они
   не разбирают.

## Common mistakes

| Ошибка | Как правильно |
|---|---|
| Вставка диффа в промпт | Codex сам читает репо |
| `--approve-for-me`, `--dangerously-bypass-*`, `-s workspace-write`, `-s danger-full-access` | Запрещены: ревью только на чтение |
| Длинный цикл заведён без `consilium-refach.md` | Файл замечаний обязателен в длинном цикле — правило и состав в `references/cycle-state.md`, «Файл замечаний по скиллу» |
